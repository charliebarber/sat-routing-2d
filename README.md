2D simulator built with matplotlib and NetworkX to prototype satellitte routing algorithm.

## Snapshots

In the snapshot files,
- `Node -1` represents a ground station in LDN
- `Node -2` represents a ground station in NYC